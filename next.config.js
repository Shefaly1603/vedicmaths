/** @type {import('next').NextConfig} */
const nextConfig = {
   // keep this empty for now; add options later only if needed
  images: { unoptimized: true }, // helps reduce build work/memory
};

module.exports = nextConfig;
